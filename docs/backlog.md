# 후추덕배 디펜스 백로그

작성일: 2026-07-21

## 완료

### HD-BL-007 오프리시 보호자 이동 중 순간 소실·도약 수정

- 상태: 완료 (2026-07-21)
- 현상: 오프리시 보호자가 이동 중 잠깐 사라졌다가 진행 방향으로 조금 이동한 위치에서 다시 나타나 순간이동하거나 도약하는 것처럼 보인다.
- 요구사항: 오프리시 보호자의 렌더링과 걷기 애니메이션을 실제 이동 좌표에 연속적으로 연결해 순간 소실과 위치 점프를 제거한다.
- 완료 조건:
  - 정상적인 등장·사망·화면 밖 퇴장 상태를 제외하면 활성 오프리시 보호자가 이동 프레임 사이에 사라지지 않는다.
  - 연속 프레임의 화면 위치가 기존 이동 속도와 경로를 따르며, 한 프레임 이상 건너뛴 것처럼 보이는 위치 점프가 없다.
  - 남성·여성 에셋과 모든 진입 방향에서 걷기 프레임, 좌우 반전, 투명도 및 크기가 끊김 없이 유지된다.
  - 390×844 모바일 화면과 데스크톱 화면에서 동일 현상이 재발하지 않는다.
  - 활성 적 스냅샷과 렌더 액터의 가시성·위치 연속성을 자동화 회귀 테스트로 고정한다.
- 구현 근거: 4초마다 진행도를 64px 즉시 더하던 대시 경계를 제거하고, 동일한 평균 이동량을 초당 16px의 연속 보너스로 분산했다. 감속·ETA도 같은 연속 이동 모델을 사용하며 4초 경계를 통과하는 고정 프레임 회귀 테스트로 위치 점프가 없음을 확인했다.

### HD-BL-008 꼬리치기 동심원 제거 및 마지막 동작 강조

- 상태: 완료 (2026-07-21)
- 요구사항: 꼬리치기 발동 시 나타나는 동심원 형태의 방사형 효과를 제거하고, 후추가 꼬리를 휘두르는 동작을 명확하게 볼 수 있도록 공격 애니메이션의 마지막 프레임을 0.8초간 유지한다.
- 완료 조건:
  - 꼬리치기 시 후추 주변에 동심원, 원형 파동 또는 반복되는 원 테두리 효과가 나타나지 않는다.
  - 꼬리를 가장 크게 휘두른 마지막 프레임이 정확히 800ms 동안 화면에 유지된다.
  - 마지막 프레임 유지가 끝나면 현재 이동 상태에 맞는 걷기 또는 대기 애니메이션으로 자연스럽게 복귀한다.
  - 프레임 유지 중에도 이동 입력, 적 이동, 다른 자동 공격과 게임 시간은 멈추지 않는다.
  - 피해량, 판정 범위, 중앙 바깥 방향 넉백, 둔화 및 쿨다운은 변경하지 않는다.
  - 390×844 모바일 화면에서 꼬리 휘두르기 자세를 분명히 알아볼 수 있고, 효과 제거와 800ms 유지 시간을 자동화 테스트로 고정한다.
- 구현 근거: `CombatEffectPool`과 `GameScene`에서 꼬리치기 원형 호·원형 먼지 생성 경로를 제거했다. 애니메이션 시트의 마지막 프레임 시작 시각에 800ms를 더해 몸동작 종료 시간을 계산하며, 마지막 프레임과 Scene 배선을 단위 테스트로 고정했다.

### HD-BL-009 보스 이동속도 20% 증가

- 상태: 완료 (2026-07-21)
- 요구사항: 개장수와 불법번식장 운영자의 실제 이동속도를 현재 값의 1.2배로 높인다.
- 완료 조건:
  - 개장수와 불법번식장 운영자의 초당 이동 거리가 각각 기존 대비 정확히 20% 증가한다.
  - 보스의 12시 방향 등장 위치, 이동 경로, 출현 시점과 공격 준비·피해 수치는 변경하지 않는다.
  - 걷기 애니메이션이 증가한 이동속도와 자연스럽게 맞으며 미끄러지는 것처럼 보이지 않는다.
  - 개장수와 대각선 뒤 트럭의 간격·방향·전환 연출이 이동 중에도 안정적으로 유지된다.
  - 두 보스의 기존 속도 대비 1.2배 적용과 경로 도착 시간 단축을 자동화 테스트로 고정한다.
- 구현 근거: 개장수와 불법번식장 운영자의 속도를 각각 25→30, 23→27.6으로 변경했다. 공용 보스 이동 애니메이션 배율 1.2를 일반 보스 액터와 개장수 사람·트럭 리그에 함께 적용해 이동과 발걸음의 체감을 맞췄다.

### HD-BL-010 기술 습득 직후 첫 쿨타임을 1초로 단축

- 상태: 완료 (2026-07-21)
- 요구사항: 꼬리치기, 아쿠아빔, 안전신문고를 새로 배운 직후에는 첫 자동 시전까지의 쿨타임을 1초로 시작한다.
- 완료 조건:
  - 기술 습득이 확정된 시점의 첫 쿨타임은 정확히 1,000ms이며 습득 즉시 시전되지는 않는다.
  - 유효한 대상이 있으면 습득 후 1초가 지난 첫 시뮬레이션 프레임에 해당 기술이 자동 시전된다.
  - 1초가 지났을 때 대상이 없으면 준비 상태를 유지하고, 이후 대상이 생기는 즉시 기존 자동 조준 규칙으로 시전한다.
  - 첫 시전 이후부터는 HD-BL-011이 최종 확정한 꼬리치기 6초, 아쿠아빔 10초, 안전신문고 19초의 쿨타임을 사용한다.
  - 하단 기술 버튼과 좌상단 자동 기술 HUD가 습득 직후 1초 쿨타임을 일관되게 표시한다.
  - 게임 진행을 멈추지 않는 기존 습득 방식과 간식 비용, 피해량, 범위 및 대상 선정 규칙은 변경하지 않는다.
  - 세 기술 각각에 대해 999ms 이전 미시전, 1,000ms 경계 시전과 이후 정상 쿨타임 복귀를 자동화 테스트로 고정한다.
- 구현 근거: `SkillSystem`이 학습 직후 1,000ms 쿨타임 창을 사용하고, 유효한 대상에 대한 첫 성공 시전 뒤에만 기술별 일반 쿨타임 창으로 전환하도록 변경했다. 대상이 없으면 준비 상태를 유지하며 HUD 진행률도 현재 창을 기준으로 계산한다.

### HD-BL-011 꼬리치기·안전신문고 쿨타임 및 비용 조정

- 상태: 완료 (2026-07-21)
- 요구사항: 꼬리치기 쿨타임을 2초 줄이고, 안전신문고 쿨타임을 3초 줄이는 대신 안전신문고의 습득 비용을 간식 5개 높인다.
- 완료 조건:
  - 꼬리치기의 첫 시전 이후 정상 쿨타임은 기존 8초에서 6초로 변경된다.
  - 안전신문고의 첫 시전 이후 정상 쿨타임은 기존 22초에서 19초로 변경된다.
  - HD-BL-010의 기술 습득 직후 첫 쿨타임 1초 규칙은 두 기술에도 그대로 적용된다.
  - 안전신문고 비용은 선택 당시 습득 순서의 기본 비용보다 간식 5개 비싸며, 첫째·둘째·셋째로 배우면 각각 20개·30개·45개가 필요하다.
  - 안전신문고를 배운 다음 기술의 기본 비용 단계는 기존처럼 한 단계만 진행하며 추가 5개가 다음 기술 비용에 누적되지 않는다.
  - 아쿠아빔 쿨타임과 꼬리치기·아쿠아빔의 습득 비용, 모든 기술의 피해량·범위·대상 선정 규칙은 변경하지 않는다.
  - 하단 기술 버튼, 좌상단 자동 기술 HUD, 구매 결과와 실제 간식 차감이 변경된 비용·쿨타임을 동일하게 표시한다.
  - 세 가지 습득 순서별 안전신문고 비용과 6초·19초 쿨타임 경계를 자동화 테스트로 고정한다.
- 구현 근거: 꼬리치기와 안전신문고의 일반 쿨타임을 6,000ms와 19,000ms로 변경했다. 공용 구매 비용 함수가 안전신문고에만 현재 기본 단계보다 5개를 더하며, 하단 독 표시·구매 가능 판정·구매 큐·실제 차감이 같은 20/30/45 규칙을 사용한다. 정책 전용 신규 테스트는 추가하지 않고 기존 회귀 기대값만 갱신했다.

### HD-BL-005 짖기·아쿠아빔 발사 원점을 후추 입으로 변경

- 상태: 완료 (2026-07-21)
- 요구사항: 짖기와 아쿠아빔(워터빔)의 시각 효과 및 공격 시작점을 후추의 몸 중심이 아닌 입 위치로 변경한다.
- 완료 조건:
  - 짖기 부채꼴과 아쿠아빔이 후추의 현재 입 위치에서 시작한다.
  - 후추가 어느 방향을 바라보거나 공격 애니메이션의 프레임이 바뀌어도 입 위치와 효과가 자연스럽게 연결된다.
  - 자동 조준, 공격 범위, 대상 선정, 피해량, 시전 시점과 쿨다운은 변경하지 않는다.
  - 모바일 화면에서 빔이나 짖기 효과가 후추의 몸을 가로질러 시작하거나 입과 분리되어 보이지 않는다.
- 구현 근거: `PlayerView`가 대상 좌우에 따라 후추를 반전하고 발 위치를 입 위치로 변환하며, `GameScene`은 짖기와 아쿠아빔의 시각 효과에만 변환 좌표를 전달한다. 좌우 입 좌표와 두 공격의 배선을 단위 테스트로 고정하고 390×844 브라우저에서 두 공격 프레임을 확인했다.

### HD-BL-006 게임 영역의 텍스트 선택 및 길게 누르기 방지

- 상태: 완료 (2026-07-21)
- 요구사항: 게임을 드래그하거나 길게 누를 때 텍스트가 선택되어 조작을 방해하지 않도록 한다.
- 완료 조건:
  - 게임 루트, HUD, 기술 버튼과 안내 문구에서 마우스·터치 드래그로 텍스트가 선택되지 않는다.
  - iOS 모바일 브라우저에서 길게 누르기 선택 메뉴와 터치 콜아웃이 나타나지 않는다.
  - 조이스틱 드래그, 기술 버튼 탭, 키보드 포커스와 접근성 이름은 기존처럼 동작한다.
  - 게임 바깥의 일반 문서 영역에는 텍스트 선택 방지 규칙을 적용하지 않는다.
- 구현 근거: `#game-root`와 자손에만 `user-select: none`과 `-webkit-touch-callout: none`을 적용했다. CSS 범위 단위 테스트와 390×844 브라우저의 빈 선택 문자열, 기술 버튼 포커스·클릭·접근성 이름을 검증했다.

### HD-BL-001 보스 출현 방향을 12시로 고정

- 상태: 완료 (2026-07-21)
- 요구사항: 모든 보스는 맵의 12시 방향 진입 지점에서 출현한다.
- 완료 조건:
  - 개장수와 불법번식장 운영자가 항상 12시 방향에서 출현한다.
  - 일반 적의 출현 위치와 경로 선택 규칙은 변경하지 않는다.
  - 보스의 이동, 방향 전환, 공격 및 개장수 트럭 연출이 12시 진입 경로에서 자연스럽게 이어진다.
  - 보스 종류별 출현 위치를 자동화 테스트로 고정한다.
- 구현 근거: 보스 경로를 `P3`로 고정하고 여러 시드에서 개장수·불법번식장 운영자의 출현 경로를 단위 테스트로 검증했다.

### HD-BL-002 꼬리치기 애니메이션 가시성 강화

- 상태: 완료 (2026-07-21)
- 요구사항: 꼬리치기 애니메이션의 표시 크기를 현재의 2.5배로 확대하고, 재생 속도를 현재의 0.5배로 낮춘다.
- 완료 조건:
  - 시각 효과의 너비와 높이가 현재 기준 2.5배로 표시된다.
  - 재생 속도는 0.5배가 되어 전체 표시 시간은 현재의 2배가 된다.
  - 모바일 화면에서 후추·덕배와 적에게 가려지지 않고 타격 방향을 알아볼 수 있다.
  - 피해량, 넉백, 둔화, 공격 판정 범위와 쿨다운은 변경하지 않는다.
- 구현 근거: 꼬리치기 원호와 먼지 효과를 2.5배로 확대하고 표시 시간을 250ms에서 500ms로 늘렸으며, 전투 수치는 변경하지 않았다.

### HD-BL-003 안전신문고 시각 효과를 `신고!` 글자로 교체

- 상태: 완료 (2026-07-21)
- 요구사항: 안전신문고 발동 시 종이 고지서 효과를 제거하고, 대상마다 굵은 주황색 한글 `신고!`를 표시한다.
- 완료 조건:
  - 기존 종이 및 도장 형태의 시각 효과가 나타나지 않는다.
  - 안전신문고 대상 각각의 라벨 위에 정확히 `신고!`가 굵은 주황색으로 표시된다.
  - 글자는 모바일 화면에서도 즉시 읽을 수 있고, 피해 적용 시점을 분명하게 보여준다.
  - 맵 전체 대상 선정, 일반 적·보스 피해량 및 자동 시전 규칙은 변경하지 않는다.
- 구현 근거: 공유 전투 아틀라스에 굵은 주황색 `신고!` 글리프를 추가해 대상별로 표시하고, 모바일 화면에서 캐릭터 및 이름표와 겹치지 않는 위치를 확인했다.

### HD-BL-004 꼬리치기 넉백을 중앙 바깥 방향으로 통일

- 상태: 완료 (2026-07-21)
- 요구사항: 꼬리치기에 맞은 적이 보호소가 있는 맵 중앙에서 멀어지는 방향으로 밀려난다.
- 완료 조건:
  - 실제 적 위치는 기존 진입 경로를 따라 중앙 바깥쪽으로 35px 이동한다.
  - 순간 리코일 애니메이션은 보호소 중심에서 피격 적 위치로 향한다.
  - 적이 보호소 중심과 같은 좌표이면 12시 방향을 기본값으로 사용한다.
  - 피해량, 둔화, 판정 범위, 쿨다운과 다른 공격의 피격 방향은 변경하지 않는다.
- 구현 근거: 꼬리치기 피해에만 보호소 중심 기준 정규화 방향을 적용하고, 중심 좌표 기본값과 비꼬리 공격 회귀를 단위 테스트로 검증했다.
